Pe ubuntu:

se instaleaza compilatorul de C, C++ si librariile necesare folosind: 

sudo apt-get install gcc g++ libssl-dev

downloadam libmosquitto

dezarhivam:

tar xvzf libmosquitto.tar.gz

cd libmosquitto

make 
sudo make install 


 
